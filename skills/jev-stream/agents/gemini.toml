# Code generated by jev skillgen from skills/jev-stream/skill.json. DO NOT EDIT.

description = "Run jev over many records with -i jsonl, -i lines or -i request. Covers -j concurrency, --merge, per record error records, jq consumption and freezing a run for replay. Use when evaluating a file of tickets, rows or diffs, building a pipeline that reads jev output, or debugging a batch run that exits 6 or 3."
prompt = "1. Read .error before you read any answer.\n2. Output is one line per input line, in input order, whatever -j is.\n3. Exit 6 means some records failed and every line is still present.\n4. --merge needs a structured output, so not table and not raw.\n5. Freeze a run with --print-request and replay it with -i request."
