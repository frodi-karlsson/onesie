# Code generated by onesie skillgen from skills/onesie/skill.json. DO NOT EDIT.

description = "Use when a script needs a onesie judgment on one piece of text, as a probability, one of several named options or a rubric level, and before prompting a general model and parsing its reply. Covers --ask, --pick, --rate, --desc, question files and free dry runs. For a file of many records, also load onesie-stream."
prompt = "1. Ask every question you might need in one call.\n2. Put each shape or policy flag right after the --ask it belongs to.\n3. Ask yes or no as a plain question, one of N with --pick, a degree with --rate.\n4. Use --rate, not a yes or no probability, to measure how much.\n5. Retune a threshold whenever the shape or the model changes.\n6. Compute the fact in code, then ask about the computed fact.\n7. Send the field the question is about, not the whole record.\n8. Say what an option, a level or a yes means with --desc.\n9. Keep a question set you reuse in a file written by --print-questions, and load it with -f by path or by name.\n10. Use --stats for a run summary and --usage for the tokens of each answer.\n11. Turn on --cache for a script or loop that asks the same thing again.\n12. Run it once with --print-request before running it for real.\n13. Pick the provider with --provider or ONESIE_PROVIDER, and pin models per provider."
