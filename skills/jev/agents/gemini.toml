# Code generated by jev skillgen from skills/jev/skill.json. DO NOT EDIT.

description = "Use when a script needs a jev judgment on one piece of text, as a probability, one of several named options or a rubric level, and before prompting a general model and parsing its reply. Covers --ask, --pick, --rate and free dry runs. For a file of many records, also load jev-stream."
prompt = "1. Ask every question you might need in one call.\n2. Put each shape or policy flag right after the --ask it belongs to.\n3. Ask yes or no as a plain question, one of N with --pick, a degree with --rate.\n4. Use --rate, not a yes or no probability, to measure how much.\n5. Retune a threshold whenever the shape or the model changes.\n6. Compute the fact in code, then ask about the computed fact.\n7. Send the field the question is about, not the whole record.\n8. Run it once with --print-request before running it for real."
