# Code generated by onesie skillgen from skills/onesie-gate/skill.json. DO NOT EDIT.

description = "Use when a onesie answer decides whether a command runs, a step continues, a build fails or a low confidence answer goes to a human, and before writing 'if onesie', 'onesie ... && cmd', -q or --assert, or picking the cut a gate uses. One common gate recipe looks right and is silently wrong."
prompt = "1. Phrase the question so that true means go, not stop.\n2. Calibrate the question against labelled records before you pick a --threshold or an --assert cut.\n3. Beside --assert, -q only silences the output. The assertion alone sets the exit code.\n4. Treat every non zero exit as no, not just exit 1.\n5. Use --abstain-if for the middle ground, and branch on 0, 1 and 7.\n6. -q on its own gates one question on one record. Add --assert for several questions, and use --assert alone for a stream.\n7. Gate a pick or rate on --min-confidence and give --fallback a human routed value.\n8. Check an assertion with --print-questions before you spend anything.\n9. Test every branch of a gate script with --mock before it spends anything.\n10. Read value, confidence, p, score, norm, decision and fallback, and compare with == != < <= > >= and in.\n11. Use min, max, sum and avg in --assert, and jq for anything else.\n12. Write an id with a space, dot or hyphen as [\"my id\"].value."
