# Code generated by onesie skillgen from skills/onesie-gate/skill.json. DO NOT EDIT.

description = "Use when a onesie answer decides whether a command runs, a step continues, a build fails or a low confidence answer goes to a human, and before writing 'if onesie', 'onesie ... && cmd', -q or --assert. One common gate recipe looks right and is silently wrong."
prompt = "1. Phrase the question so that true means go, not stop.\n2. Beside --assert, -q only silences the output. The assertion alone sets the exit code.\n3. Treat every non zero exit as no, not just exit 1.\n4. -q on its own gates one question on one record. Add --assert for several questions, and use --assert alone for a stream.\n5. Gate a pick or rate on --min-confidence and give --fallback a human routed value.\n6. Check an assertion with --print-questions before you spend anything.\n7. Do the arithmetic in jq, not in --assert.\n8. Write an id with a space, dot or hyphen as [\"my id\"].value."
