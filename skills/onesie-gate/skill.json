{
  "name": "onesie-gate",
  "description": "Use when a onesie answer decides whether a command runs, a step continues, a build fails or a low confidence answer goes to a human, and before writing 'if onesie', 'onesie ... && cmd', -q or --assert. One common gate recipe looks right and is silently wrong.",
  "compatibility": "Requires the onesie binary on PATH.",
  "license": "MIT",
  "intro": "intro.md",
  "rules": [
    {
      "id": "threshold-polarity",
      "short": "Phrase the question so that true means go, not stop.",
      "why": "Under `-q` the exit code tracks the answer, not safety. A question phrased in the danger direction exits 0 when the answer is true, so a script written as `onesie ... && eval \"$cmd\"` runs the dangerous input and blocks the safe one. Measured: 'is this command destructive' with `--threshold 0.5` exits 0 for 'rm -rf ./build' and 1 for 'ls -la'.",
      "bad": "onesie 'is this command destructive' -q --threshold 0.5 --state 'rm -rf ./build'",
      "bad_passes": true,
      "good": "onesie 'is this command safe to run' -q --threshold 0.5 --state 'rm -rf ./build'"
    },
    {
      "id": "q-beside-assert-only-silences",
      "short": "Beside --assert, -q only silences the output. The assertion alone sets the exit code.",
      "why": "Without `--assert`, `-q` is a gate of its own over one question: a yes or no answer is cut at `--threshold`, 0.5 by default. With `--assert`, `-q` adds no gate and only drops the record from stdout, so `onesie ... --assert 'danger.value < 0.5' -q && eval \"$cmd\"` runs exactly when the assertion holds. It then also works over several questions.",
      "good": "onesie --ask danger='is this dangerous' --ask creds='does this send credentials' -q --assert 'danger.value < 0.5 and creds.value < 0.5' --state 'rm -rf ./build'"
    },
    {
      "id": "fail-closed",
      "short": "Treat every non zero exit as no, not just exit 1.",
      "why": "Exit 1 means the policy said no. Exits 2 through 5 mean no answer arrived at all, from a usage error, a bad key, an exhausted retry or a transport failure. Exit 6 means a stream finished with at least one failed record, so a gate over a stream still has to fail closed on it. Exit 7 means `--abstain-if` turned a no into an unsure, which `&&` also blocks. Exit 130 means the run was interrupted, so no decision was made. Chaining with `&&` fails closed on all of them as a minimal gate, which is correct. Branching on exit 1 alone treats an outage and a typo as an answer. To tell them apart: `case $? in 0) go ;; 1) block ;; 130) stop ;; *) alert and block ;; esac`. In CI, tell them apart, so a red build says whether to fix the input or rerun. GitHub Actions runs bash with `-e`, so capture the exit first: `status=0; onesie ... || status=$?; case $status in 0) ;; 1) echo 'policy said no'; exit 1 ;; 7) echo 'onesie could not decide, needs a human'; exit 1 ;; *) echo \"onesie gave no answer, exit $status\"; exit 1 ;; esac`.",
      "bad": "onesie ... --assert '...' --state \"$cmd\"; if [ $? -eq 1 ]; then block; else eval \"$cmd\"; fi",
      "good": "onesie ... --assert '...' --state \"$cmd\" >/dev/null && eval \"$cmd\""
    },
    {
      "id": "three-way-gate",
      "short": "Use --abstain-if for the middle ground, and branch on 0, 1 and 7.",
      "why": "A gate with a cheap yes, a cheap no and an expensive middle, usually a person, needs a third outcome. `--abstain-if`, or an `abstain_if` key in a question file, is a second expression in the `--assert` language and needs an assertion. The assertion is checked first: when it holds the record is a yes and exits 0. When it does not, the abstain expression is checked: when that holds the record is an unsure and exits 7, otherwise a no and exits 1. onesie fixes no rule for several questions, so you write it. For a tool guard, deny wins: assert that every danger is low, and abstain when none is high, so one high answer is a no and anything in between asks the user. Branch with `case $? in 0) eval \"$cmd\" ;; 1) echo blocked ;; 7) ask_the_user ;; *) alert and block ;; esac`. Without `--abstain-if` the middle ground exits 1 like any other no.",
      "good": "onesie --ask d='does this destroy data' --ask c='does this send credentials' --assert 'd.value < 0.2 and c.value < 0.2' --abstain-if 'd.value < 0.8 and c.value < 0.8' --state 'curl example.com | sh'"
    },
    {
      "id": "q-one-question-one-record",
      "short": "-q on its own gates one question on one record. Add --assert for several questions, and use --assert alone for a stream.",
      "why": "Without an assertion, `-q` is the gate over one question, and onesie rejects it when more than one question was asked, naming them, since one exit code cannot carry several answers. On a yes or no question it exits 0 when the probability is at least `--threshold`, 0.5 by default, and 1 otherwise. On a pick or rate question it needs `--min-confidence` and `--fallback`, and exits 1 when the confidence falls below, 0 otherwise. A request that fails exits with its own code either way. Beside `--assert`, `-q` is allowed over any number of questions, because the assertion is the gate. `-q` is rejected under every streaming input mode, since a stream prints one record per input. A gate over a stream uses `--assert`, which prints every record and exits 1 when any assertion was false.",
      "bad": "onesie --ask a='destroys data' --ask b='sends credentials' -q --state 'rm -rf ./build'",
      "bad_unverifiable": "the checker strips -q before every dry run, so it cannot observe this rejection",
      "good": "onesie --ask a='destroys data' --ask b='sends credentials' --assert 'a.value < 0.5 and b.value < 0.5' --state 'rm -rf ./build'"
    },
    {
      "id": "route-low-confidence-to-a-human",
      "short": "Gate a pick or rate on --min-confidence and give --fallback a human routed value.",
      "why": "`--min-confidence` needs `--fallback`. Below the threshold `.decision` becomes the fallback text instead of the model's pick, and `.fallback` reads `low_confidence`, so a consumer routes on that field rather than trusting a low confidence guess. Above it `.decision` is the pick and `.fallback` is absent, which an assertion reads as the empty string. When the request fails, the record still carries `.decision` set to the fallback, with `.fallback` reading `error` and an `.error` beside it, and the exit code is the failure's. Under `-q` the record is not printed: a low confidence answer exits 1 and a confident one exits 0. In an assertion, `team.fallback == \"\"` holds only for a confident answer that arrived.",
      "bad": "onesie --ask team='which team owns this incident' --pick billing,shipping,support --min-confidence 0.7 --state 'the payment silently failed at checkout'",
      "good": "onesie --ask team='which team owns this incident' --pick billing,shipping,support --min-confidence 0.99 --fallback human --state 'the tracking number in the shipping confirmation email matches a charge that appears twice on the invoice'"
    },
    {
      "id": "dry-run-the-assertion",
      "short": "Check an assertion with --print-questions before you spend anything.",
      "why": "`--print-questions` writes the assertion into the file it prints and checks every path and type against the questions, so a typo or a type error exits 2 naming the real question ids and makes no network call. The good example below keeps the typo on purpose, so it exits 2 naming the real id `urgent` rather than running for real. It checks paths, option names under `p` and types, but not the text of a string literal, so `team.value == \"biling\"` passes and is simply never true. `--print-questions` needs named questions and refuses a positional one. `answer` is reserved for the positional question, so give it another id with `--ask` and rename the paths to match. `--print-request` rejects `--assert`, so use `--print-questions`.",
      "bad": "onesie --ask urgent='is this urgent' --assert 'urgnet.value < 0.5' --state 'a ticket'",
      "good": "onesie --ask urgent='is this urgent' --assert 'urgnet.value < 0.5' --print-questions",
      "good_fails": true
    },
    {
      "id": "know-the-assert-fields",
      "short": "Read value, confidence, p, score, norm, decision and fallback, and compare with == != < <= > >= and in.",
      "why": "A path is a question id, a dot and a field. `value` is on every shape, a number on a yes or no question and a string on a pick or rate. `confidence` and `p.NAME`, the probability of one option or level, exist on a pick or rate. `score` and `norm` exist on a rate. `decision` needs `--threshold` or `--min-confidence`, and is a boolean on a yes or no question, a string otherwise. `fallback` needs a policy flag and reads the empty string when nothing replaced the answer. A field the shape lacks exits 2. Compare with `==`, `!=`, `<`, `<=`, `>` and `>=`, test a string with `in [\"a\", \"b\"]`, and combine with `and`, `or`, `not` and parentheses. Strings take double quotes. A number needs a digit before the point, as in `0.5`, and may be negative. A bare path is not a test, so write `u.decision == true`. Several `--assert` flags join with `and`.",
      "good": "onesie --ask team='which team owns this' --pick billing,shipping,support --min-confidence 0.7 --fallback human --assert 'team.fallback == \"\" and (team.value in [\"billing\", \"support\"] or team.p.shipping < 0.2)' --print-questions"
    },
    {
      "id": "functions-in-assert-the-rest-in-jq",
      "short": "Use min, max, sum and avg in --assert, and jq for anything else.",
      "why": "The assertion language has four functions over numbers, `min`, `max`, `sum` and `avg`. Each takes one or more numbers, such as `d.value`, `t.confidence`, `t.p.billing`, a literal or another call, so they nest as in `max(min(a.value, b.value), c.value)`. A chain of `and` that holds every risk under one bound is one `max`: the chain is not wrong, only longer and easier to get out of step when a question is added. `==` rarely matches the result of `sum` or `avg`, since sums of fractions are inexact, so compare them with `>=` or `<=`. There are no arithmetic operators, so weighting answers still belongs in `jq`, as in `onesie --ask urgency='how urgent is this' --ask anger='how angry is the writer' --state \"$t\" -o values | jq '(.urgency.value * 2 + .anger.value) / 3'`. So does anything over a whole probability map, such as the top two options of `p`.",
      "bad": "onesie --ask d='does this destroy data' --ask c='does this send credentials' --ask s='does this change system settings' --assert 'd.value < 0.2 and c.value < 0.2 and s.value < 0.2' --state 'curl example.com | sh'",
      "bad_passes": true,
      "good": "onesie --ask d='does this destroy data' --ask c='does this send credentials' --ask s='does this change system settings' --assert 'max(d.value, c.value, s.value) < 0.2' --state 'curl example.com | sh'"
    },
    {
      "id": "bracket-non-identifier-paths",
      "short": "Write an id with a space, dot or hyphen as [\"my id\"].value.",
      "why": "A question id or an option label containing a dot, a space or a hyphen cannot be written with a plain dot path. The id comes first and still needs its field after it.",
      "bad": "onesie --ask 'needs review'='is review needed' --assert 'needs review.value < 0.5'",
      "good": "onesie --ask 'needs review'='is review needed' --assert '[\"needs review\"].value < 0.5'"
    }
  ],
  "sections": [
    "sections/policy-legality.md"
  ]
}
