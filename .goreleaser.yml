version: 2

project_name: onesie

before:
  hooks:
    - go mod tidy
    # A hook runs without a shell, so the redirect needs sh -c.
    - sh -c 'rm -rf completions && mkdir completions && for shell in bash zsh fish; do go run ./cmd/onesie completion "$shell" > "completions/onesie.$shell"; done'

builds:
  - id: onesie
    main: ./cmd/onesie
    binary: onesie
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .CommitDate }}
      - -X main.tag={{ if not .IsSnapshot }}{{ .Tag }}{{ end }}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    mod_timestamp: "{{ .CommitTimestamp }}"

# OFF until the MACOS_SIGN_P12 secret exists. Then the darwin binaries are
# signed and notarized before they are archived. The block follows
# notarize.macos in the GoReleaser v2.18.2 docs, which signs through quill
# and so runs on the ubuntu release runner.
notarize:
  macos:
    - enabled: '{{ isEnvSet "MACOS_SIGN_P12" }}'
      ids: [onesie]
      sign:
        # The base64 of the Developer ID Application .p12, and its password.
        certificate: "{{ .Env.MACOS_SIGN_P12 }}"
        password: "{{ .Env.MACOS_SIGN_PASSWORD }}"
      notarize:
        # The App Store Connect API key: its issuer UUID, its key ID and the
        # base64 of its .p8 file.
        issuer_id: "{{ .Env.MACOS_NOTARY_ISSUER_ID }}"
        key_id: "{{ .Env.MACOS_NOTARY_KEY_ID }}"
        key: "{{ .Env.MACOS_NOTARY_KEY }}"
        wait: true
        # The most GoReleaser allows, since Apple rejects longer lived tokens.
        timeout: 20m

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    # NOTICE is not among the files an archive takes by default.
    files: [LICENSE, NOTICE, README.md, completions/*]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  # Not a draft: the formula pushed to homebrew-tap points at this release's
  # assets, which stay unreachable while the release is unpublished.
  draft: false
  prerelease: auto
